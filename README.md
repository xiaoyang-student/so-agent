  这个项目还是存在很多问题，关于多agent，我的想法就是，还是两层结构，主管Agent和子Agent，将代码Agent，评审Agent，生成子Agent的Agent三个封装成工具，主管Agent拥有分配任务以及工具的权利，主管将问题拆分，调用一次评审Agent，观点相符，调用生成子Agent工具，对应各自的子问题去处理，同时主管根据每个任务下发工具，如果子Agent觉得工具不够用，可以向主管Agent申请，子Agent之间禁止通讯，主管Agent下发任务之前会检查是否有相应的工具，没有就调用代码Agent去生成，放在沙箱中测试，src\so_agent\tool_packages\generated_tools用来存放测试好的工具，具体直接看md文件即可，但是对此并不是我的最终设计，因为首先，沙箱应该禁止联网，可是代码Agent写出来的工具不会全都是不连接网络的，所以应该设置一些白名单，但是只要开了口子，就不能保证完全的安全，其次关于工具，生成的工具并不能直接存在工具库中，应该是调用后，将工具的思路存在redis里，并设置失效时间，避免资源占用，当他是一次性工具的时候，到了失效时间就自动清理，当他下一次调用的时候，我们在redis中可以记录调用次数，来决定是否需要持久化，也是为了会更好的节约token，其次对于更好的测试，我们可以将每次测试记录存在MySQL等数据库中，新建表来统计，比如一个问题会被分成几个子任务，以及需要哪些工具，生成工具，测试工具的时长，解决问题的时长，评审Agent和主管Agent之间会经历几次辩论，意见不统一是什么原因，可以更好的去写prompt，其次一定要避免无限循环，要有兜底机制，我目前逻辑写的很简单，写了人工兜底但是没有实现，我觉得解决问题一定不要把战线拉长，最多不要超过3层结构，其次现在整个解决问题的思路都是交给LLM处理，是否要求LLM具备更强大的处理问题能力，尤其是解决问题的思维，最后，我还在思考，人工智能出现的初衷是体验感更好，更顺手的工具，还是真真实实的替代人工。
